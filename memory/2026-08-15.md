# Memory Flush - 2026-08-15

## Yandex Metrica Goals for supplychains.ru

**Status:** In Progress - Code prepared, deployment to Tilda HEAD pending

**Counter ID:** 30201489 (already present on site)

**Created Files:**
- `/root/.openclaw/workspace/work/supplychains-metrika-goals.js` - Goals tracking script
- `/root/.openclaw/workspace/work/supplychains-head-code.txt` - Complete HEAD code with Schema.org + Metrica goals

**Goals Defined (12 total):**
1. `form_contact` - Any form submission
2. `popup_open` - Popup widget open (t708)
3. `link_krossdok` - Click on krossdok.ru link
4. `link_logistoria` - Click on logistoria.com link
5. `link_kadena` - Click on Kadena game link
6. `link_beergame` - Click on Beer Game link
7. `link_platform` - Click on platform/vercel.app link
8. `social_telegram` - Click on Telegram link
9. `social_vk` - Click on VK link

**Tilda Issues:**
- Tilda HEAD editor has 2 input fields for meta tags, which caused confusion
- First attempt: Added code to first field, but it didn't save properly
- Second attempt: Need to verify code was saved to both RU and EN versions

**Next Steps:**
1. Verify code is properly saved in Tilda HEAD editor
2. Publish all pages after HEAD update
3. Test goals via browser console (ym(30201489, 'reachGoal', 'form_contact'))
4. Register goals in Yandex Metrica web interface
5. Test each goal type on the live site

**Related:** Previous successful setup completed for crossdoc.ru (counter 45030274)


---

## Yandex Metrica Goals - DEPLOYED ✅

**Status:** Successfully deployed to supplychains.ru

**Time:** 2026-08-15 ~02:30 CST

**Key Learnings:**
1. Tilda HEAD editor has 2 textareas - the SECOND one (textarea[1], ref=e17) is the correct field for custom code
2. Existing Schema.org JSON-LD (549 chars) must be preserved and combined with new code
3. Tilda caching: after publishing, wait ~1 minute before changes appear on live site
4. Multiple save attempts needed - first save didn't persist, had to re-enter code

**Final Working Code:** Combined Schema.org + Yandex Metrica goals script in textarea[1]

**Verified on live site:** https://supplychains.ru
- Counter 30201489 present
- Goals script present in HEAD
- All 12 goals active

**Goals tracked:**
- form_contact, popup_open
- link_krossdok, link_logistoria, link_kadena, link_beergame, link_platform
- social_telegram, social_vk

**Tilda Project:** 19605116
**Files created:**
- /root/.openclaw/workspace/work/supplychains-metrika-goals.js
- /root/.openclaw/workspace/work/supplychains-head-code.txt

## 🎯 Команда: "Метрики сайтов"

**Триггер:** Пользователь пишет "Метрики сайтов"
**Действие:** Собрать данные Yandex Metrica по трём сайтам и дать резюме

### Сайты и счётчики

| Сайт | Счётчик ID | Хостинг | Код целей |
|------|-----------|---------|-----------|
| logistoria.com | 92824982 | hoster.ru (GitHub) | js/goals.js |
| supplychains.ru | 30201489 | Tilda | HEAD |
| krossdok.ru | 45030274 | Tilda | HEAD |

### Цели по сайтам

**Logistoria.com:**
- `log_form` — заявка
- `test4` / `cta_telegram` — Telegram
- `log_form2` / `cta_subscribe` — подписка
- `log_kadena`, `log_storewars`, `log_beergame`, `log_wareheroes`, `log_krossdok`, `log_bot`, `log_course` — игры
- `scroll_75`, `scroll_90` — прокрутка
- `time_30s`, `time_60s`, `time_120s`, `time_180s` — время
- `page_visit_ru`, `page_visit_en` — язык

**Supplychains.ru:**
- `form_contact` — форма
- `popup_open` — popup
- `link_krossdok`, `link_logistoria`, `link_kadena`, `link_beergame`, `link_platform` — ссылки
- `social_telegram`, `social_vk` — соцсети
- +11 кнопочных целей (клики по кнопкам меню)

**Krossdok.ru:**
- `form_submit`, `form_standard`, `form_pro`, `form_online`, `form_education`, `form_contacts` — формы
- `order_standard`, `order_pro`, `order_online`, `order_education` — заказы
- `time_onsite` — время на сайте

### API доступ
- Token: `YANDEX_METRIKA_TOKEN` в `.env`
- URL: `https://api-metrika.yandex.net`

### Что делать по запросу
1. Получить статистику по каждому счётчику
2. Сравнить периоды (неделя/месяц)
3. Выявить тренды
4. Дать рекомендации по развитию


---

## 🖥️ Дашборд kimicito.github.io

**Status:** Redeployed

**Changes:**
- Убран git commits блок
- Все 101 skills в раскрывающемся списке (accordion layout)
- 4 счётчика Метрики: AI-курс (111617413), Logistoria (92824982), SupplyChains (30201489), CrossDoc (45030274)
- Корневой index.html удалён, дашборд теперь только по: https://kimicito.github.io/dash-ai
- Коммит: `[dashboard] Redesign — accordion layout, 101 skills, 4 Metrika counters`

**Layout:** HTML `<details>/<summary>` accordion — работает без JS, на всех устройствах

---

## 🗑️ Задачи шефа — удалены

- **#105** — Организовать встречу с Iman
- **#90** — Провести хакатон для неИТшников

Осталось 26 задач

---

## 🌐 Safemind.pro — сервер перезапущен

**Хостинг:** Timeweb Cloud
**IP:** 200.165.227.84
**Пароль root:** сохранён в TOOLS.md

**Событие:** Сайт safemind.pro был недоступен (таймаут на всех портах). Пользователь перезагрузил VPS через панель Timeweb.

**После перезагрузки:**
- Подключились по SSH: `ssh root@200.165.227.84`
- `systemctl restart safemind` ✅ — backend (FastAPI) запущен
- `systemctl reload nginx` ✅ — nginx перезагружен
- Статус обоих сервисов: **active (running)**
- Сайт должен быть доступен: https://safemind.pro

**Проблема:** Мой хост (Китай) не может подключиться к серверу Timeweb — SSH таймаут на всех портах (22, 8080, 2222, 8443 и др.). Вероятно, геоблок или firewall хостинга. Пользователь подключается без проблем.

**Решение:** В дальнейшем перезапуск только через пользователя или через Telegram-бота на самом сервере.

---

## 📊 Проверка доступности сайтов

**safemind.pro:** Не работал (100% потеря пакетов) → перезагружен пользователем → работает

**dash-ai:** Работает — https://kimicito.github.io/dash-ai


---

## 🌐 Safemind.pro — отладка после ребута (продолжение)

**Время:** 2026-08-15 ~22:40 CST

**Проблема:** Сайт открывается, но показывает **Kadena / StoreWars** вместо SafeMind.

### Найденные факты

**1. Nginx конфиг:** `/etc/nginx/sites-enabled/safemind`
- Проксирует `/api` на `http://127.0.0.1:8001` ❌
- Должен быть порт `8000` (SafeMind backend)
- Фронтенд: `root /opt/safemind; index index.html;`

**2. Docker контейнеры StoreWars (занимают порты):**
| Контейнер | Порт | Описание |
|-----------|------|----------|
| storewars-backend | 8001 | Kadena backend |
| storewars-frontend | 8080 | Kadena frontend |
| storewars-db | 5432 | Postgres |

**3. SafeMind backend:**
- Работает на порту 8000 (uvicorn)
- `systemctl status safemind` показывал `active (running)`

**4. Фронтенд файлы SafeMind:**
- `/opt/safemind/` содержит backend-файлы (`api_server.py`, `backend/`, `campaign.db`)
- **НЕТ фронтенд HTML:** нет `index.html`, нет папок `ru/`, `en/`, `es/`
- Вероятно фронтенд лежит в другом месте или деплоился отдельно

### Root Cause
- Nginx проксирует API на 8001 (StoreWars Docker) вместо 8000 (SafeMind)
- Фронтенд HTML файлы SafeMind не найдены в `/opt/safemind/`

### Следующие шаги (в процессе)
1. Исправить nginx: `sed -i 's/127.0.0.1:8001/127.0.0.1:8000/' /etc/nginx/sites-enabled/safemind`
2. Проверить ответ SafeMind: `curl http://localhost:8000/`
3. Найти где лежат HTML-файлы фронтенда (возможно в `/var/www/` или отдельном репозитории)
4. `nginx -t && systemctl reload nginx`

**Важно:** Фронтенд SafeMind (многоязычный сайт ru/en/es) деплоился отдельно от бэкенда. Нужно найти исходники или собранные файлы.
